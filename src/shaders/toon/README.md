OWNER: C — toon ramp + rim/outline pass goes here.
Two files expected: `toon.vert.glsl`, `toon.frag.glsl`.
Outline via inverted-hull or a post-process depth/normal edge detect — C decides, then documents which and why.
