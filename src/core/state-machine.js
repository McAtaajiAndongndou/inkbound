// OWNER: A (Core)
// menu -> loadout -> playing -> paused -> win/lose -> restart
// Restart must work WITHOUT a page refresh (explicit rubric item under Polish).
export class StateMachine {
  constructor() { throw new Error('TODO: A'); }
}
