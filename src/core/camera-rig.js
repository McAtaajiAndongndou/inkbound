// OWNER: A (Core)
// Rubric (Viewing) wants MULTIPLE view modes. We ship three:
//   'third'  - default over-the-shoulder
//   'first'  - ADS / down-the-sights
//   'ortho'  - top-down orthographic, rendered into the minimap
// Switching modes must never snap or glitch — marker looks for this.
export class CameraRig {
  constructor() { throw new Error('TODO: A'); }
}
