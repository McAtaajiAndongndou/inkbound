// OWNER: E (UI & Audio)
// Web Audio via THREE.AudioListener. Music, splat SFX, gun, enemy, ambience.
// Sound is named inside the 25% Gameplay & Experience band — it is not optional.
// Browsers block audio until first user gesture: start the context on the
// menu's "play" click, never on page load.
export class AudioManager {
  constructor() { throw new Error('TODO: E'); }
}
